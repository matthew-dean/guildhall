{
  "name": "guildhall-repo",
  "version": "0.2.0",
  "private": true,
  "description": "Source repo for Guildhall. The publishable npm package lives at packages/guildhall/.",
  "license": "LicenseRef-FLL-1.2",
  "licenses": [
    {
      "type": "LicenseRef-FLL-1.2",
      "url": "https://fllicense.org"
    }
  ],
  "scripts": {
    "dev": "pnpm --filter @guildhall/runtime dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.1.1"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  }
}
