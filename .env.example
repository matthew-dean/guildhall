# ---------------------------------------------------------------------------
# LM Studio (primary — all local models go through this)
# ---------------------------------------------------------------------------
LM_STUDIO_BASE_URL=http://localhost:1234/v1

# ---------------------------------------------------------------------------
# Cloud API keys (optional — only needed if you assign cloud models in forge.config.ts)
# ---------------------------------------------------------------------------
# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...

# ---------------------------------------------------------------------------
# Project paths
# ---------------------------------------------------------------------------
LOOMA_PATH=/Users/matthew/git/oss/looma
KNIT_PATH=/Users/matthew/git/oss/knit

# Memory directory (default: ./memory relative to where you run forge)
FORGE_MEMORY_DIR=./memory
